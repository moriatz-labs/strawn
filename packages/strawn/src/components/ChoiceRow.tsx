import { styled } from "../stitches";

export const ChoiceRow = styled("label", {
    alignItems: "center",
    color: "$foreground",
    display: "flex",
    gap: "$3",
    fontFamily: "$body",
    fontSize: "$sm",
    lineHeight: "$snug",
    minHeight: "2.75rem",
});
