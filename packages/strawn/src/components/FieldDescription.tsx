import { styled } from "../stitches";

export const FieldDescription = styled("p", {
    color: "$mutedForeground",
    fontFamily: "$body",
    fontSize: "$sm",
    lineHeight: "$snug",
    margin: 0,
});
