import { styled } from "../stitches";

export const ButtonContent = styled("span", {
    alignItems: "center",
    display: "inline-flex",
    gap: "inherit",
    opacity: 1,
    transition: "opacity $fast",
    position: "relative",
    zIndex: 1,
});
